import React from "react";
import "./App.css";

const TestOver: React.FC = ({}) => {
  return (
    <div className="results">
      <h2>This test is over!</h2>
      {/* <p>
          Your score: {score}/{selectedQuestions.length}
        </p> */}
    </div>
  );
};

export default TestOver;
